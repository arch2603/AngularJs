app.controller('HeaderController', ['$scope', '$http', '$timeout', '$filter', function($scope, $http, $timeout, $filter) {

   /* $scope.IsVisible = false;
    $scope.isVisiblePara = true;
    $scope.statusname = "click to open";

    $scope.toggleEditor = function ()
    {
        var outcome;
        //$scope.IsVisible = $scope.IsVisible ? false : true;
        $scope.isVisiblePara = $scope.isVisiblePara ? false : true;

        if($scope.IsVisible){
            //getAllText();
            $scope.IsVisible = !$scope.IsVisible;
            $scope.statusname = "click to open";
            outcome = "close";
            //console.log("line 44");
        }else{
            $scope.IsVisible = !$scope.IsVisible;
            $scope.statusname = "click to save";
            //console.log("line 48");
            outcome = "open";
        }
    };*/
}]);